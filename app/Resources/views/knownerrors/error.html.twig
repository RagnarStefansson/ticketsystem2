{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-7">
            <ul>
                <li>Titel: {{ trouble.titel }}</li>
                <li>Kunde: {{ getCustomerName(trouble.kid)}}</li>
                <li>Tags: {{ trouble.tags }}}</li>
                <li>Beschreibung: {{ trouble.freeform }}</li>
            </ul>
        </div>
        <div class="col-md-4">
            <div class="btn-group" role="group" aria-label="...">
                <a href="{{ path('editerror', {'tid': trouble.pid}) }}" type="button" data-toggle="tooltip" data-placement="bottom" title="Fehler bearbeiten" class="btn btn-default"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                <button type="button" class="btn btn-danger" data-toggle="modal" data-toggle="tooltip" data-placement="bottom" title="Fehler Löschen" data-target="#myModal"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></button>
            </div>
        </div>
    </div>
{% endblock %}
