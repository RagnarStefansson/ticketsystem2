{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="col-md-7">
            <li>Kunde: {{ customer.name }}</li>
            <li>Straße: {{ customer.adress.street }} {{ customer.adress.number }}</li>
            <li>PLZ: {{ customer.adress.zipcode }}</li>
            <li>Ort: {{ customer.adress.city }}</li>
            <li>Telefon:{{ customer.phone }}</li>
            <li>E-Mail Adresse: {{ customer.mail }}</li>
            <li>Vertragsart: {{ getContractName(customer.vid) }}</li>
        </div>

        <div class="btn-group" role="group" aria-label="...">
            <a href="{{ path('customeredit', {'kid': customer.kid}) }}" type="button" data-toggle="tooltip" data-placement="bottom" title="Kunden bearbeiten" class="btn btn-default"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
        </div>

    </div>
{% endblock %}